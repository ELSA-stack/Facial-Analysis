// Simple input field for future forms.
function Input({ placeholder = 'Enter text' }) {
  return <input className="w-full rounded border px-3 py-2" placeholder={placeholder} />
}

export default Input
